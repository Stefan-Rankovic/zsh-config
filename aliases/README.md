# Aliases

## Description
Useful abbreviations for common commands.
For example, no need to type `trash-put`, when you could type `tp`.
Or instead of `paru -S`, you could just type `pin`.
They are just abbreviations to avoid typing; none will reference any other aliases defined here. If one ever needs to, that alias will become a function.
You can see what each alias stands for in its file.

